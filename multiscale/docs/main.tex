\documentclass{article}\usepackage{noweb}\pagestyle{noweb}\noweboptions{}\begin{document}\nwfilename{main.nw}\nwbegindocs{0}\section{Multiscale modelling}% ===> this file was generated automatically by noweave --- better not edit it

Program structure is following.

\nwenddocs{}\nwbegincode{1}\moddef{*}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
\LA{}Import\RA{}
\LA{}Functions\RA{}
\LA{}main.py\RA{}
\nwendcode{}\nwbegindocs{2}\nwdocspar

\par Dependencies and import

  Let's import all we need. We need a module for XML parsing and one for logging
  and printing debugging messages, we'll write our own module.

\nwenddocs{}\nwbegincode{3}\moddef{Import}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
\LA{}DebugModule\RA{}
\LA{}XMLModule\RA{}
\nwendcode{}\nwbegindocs{4}\nwdocspar

\nwenddocs{}\nwbegincode{5}\moddef{DebugModule}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
import debug
\nwendcode{}\nwbegindocs{6}\nwdocspar

\nwenddocs{}\nwbegincode{7}\moddef{XMLModule}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
import parser
\nwendcode{}\nwbegindocs{8}\nwdocspar

\par What we want to do 

  Lets write what we want to do in main. Currently I have nothing on my mind. So
  lets just leave it empty.

\nwenddocs{}\nwbegincode{9}\moddef{main.py}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
\LA{}imports\RA{}
print("Doing really nothing")
\nwendcode{}\nwbegindocs{10}\nwdocspar


\nwenddocs{}\nwbegincode{11}\moddef{imports}\endmoddef\nwstartdeflinemarkup\nwenddeflinemarkup
import logging 
import debug
logger = logging.getLogger('multiscale')
try:
    from lxml import etree
    debug.printDebug("DEBUG", "running with lxml.etree")
except ImportError:
    try:
        # Python 2.5
        import xml.etree.cElementTree as etree
        debug.printDebug("DEBUG", "running with cElementTree")
    except ImportError:
        try:
            # Python 2.5
            import xml.etree.ElementTree as etree
            debug.printDebug("DEBUG", "running with ElementTree")
        except ImportError:
            try:
              # normal cElementTree install
              import cElementTree as etree
              debug.printDebug("DEBUG", "running with cElementTree")
            except ImportError:
                try:
                    # normal ElementTree install
                    import elementtree.ElementTree as etree
                    debug.printDebug("DEBUG", "running with ElementTree")
                except ImportError:
                    debug.prefix("FATAL", "Failed to import ElementTree")
                    os._exit(1)

\nwendcode{}\nwbegindocs{12}\nwdocspar

\nwenddocs{}\end{document}

